class attachmentDomain {
    constructor({ downloadedFile, fileExtension, fileName }) {
      this.downloadedFile = downloadedFile;
      this.fileExtension = fileExtension;
      this.fileName = fileName;
    }
  }
  
  module.exports = attachmentDomain;
  